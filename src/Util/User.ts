class User {
    
    private socketId;

    public constructor() {

    }

    public setSocketId(socketId) {
        this.socketId = socketId;
    }

    public getSocketId() {
        return this.socketId;
    }
}